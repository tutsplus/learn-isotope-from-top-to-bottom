$( function() {
    $( ".notes" ).isotope( {
        itemSelector: ".note",
        isResizeBound: true,
        // containerStyle: { background: 'red' },
        // isInitLayout: false
    } );
} );
